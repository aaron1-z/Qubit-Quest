{
  "name": "qubit-quest-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel serve public/index.html --no-source-maps --port 1234",
    "build": "parcel build public/index.html --dist-dir dist --public-url ./"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "process": "^0.11.10",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}